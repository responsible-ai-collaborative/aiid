import { testIntegrity } from "../shared/integrity";

// TODO: The test `Classifications should be linked to one and only one incident ` is failing and the seeds need to be updated.

// testIntegrity();